# conformance-smoke handoffs

These clauses are not claimed closed. The conformance lane cannot honestly turn them into
observable end-state proofs without the named spec or out-of-lane mechanism change.

| Clause(s) | Exact blocker | Required cross-lane/spec change |
|---|---|---|
| #4 | Copying the corpus and invoking the same ExUnit loader is not a second self-tuning consumer. | The self-tuning/generated-rail owner must load `identity/conformance/` through its real validator and run the unchanged corpus. |
| #60 | Fresh identity bootstrap is implemented in `lib/tightbeam/archetypes.ex`, outside this lane. | The bootstrap owner must seed/update canonical `priv/kungfu/agentic-engineering/rails/engineering.toml` and add a fresh-identity proof. |
| #21 | §1.2 says every fixture in a green class is green, while A1 requires green C2/C3 classes containing three `pending-unhomed` fixtures. | Amend the governing spec to define whether class green ignores explicitly unhomed fixtures or change A1/taxonomy. |
| #7, #147, #155 | The governing spec declares forbidden-substitution intent advisory and unenforceable. A constant assertion cannot prove it, so it was removed. | Either keep this portion explicitly advisory in acceptance, or define an enforceable observable and name the containment/product owner that emits it. |
| #11, #48 | Green denied events exist, but the pending script/remedy/sweep fixtures cannot yet assert every declared lifecycle row. Current `Supervision` also emits no lifecycle record for busy, adjudication-hold, stranded, or duplicate suppression. | The mechanism owner must emit the spec-declared lifecycle details for every branch; then this runner can require full declared-record equality for C2–C7. |
| #15, #137, #138, #142, #156 | A C5 phase flip still needs a real `rail_exec` runner; Cap has no acting-layer clause; decision/actor runners do not cover the complete escalate/closure/consume union. | P4–P7 owners must stabilize the callable interfaces and the conformance lane must add real `rail_exec`, Cap, escalation, closure, and consume dispatch branches before any class flip. |
| #2, #25, #52, #150, #154, #158, #163, #164 | The fixed declarative-world vocabulary has no rows for remedy episodes/claim tokens/occurrences, escalation requests/rulings/waivers, holder retirement, park wakes, or a seeded holder checkout. The locked matrices therefore cannot be expressed as pure data without runner-only hidden state. | Amend §1.6 with exact pure-data row shapes and public materializers for those states, then rewrite the affected cases and make schema validation require them. |
| #36 | `producer_verdict` contains no job id, verb, or filing session, so frozen job provenance cannot be derived when more than one job may target an assignment. Hard-coded provenance was removed from the closure claim. | Amend the row shape to reference the frozen `producer_jobs` row (or remove this key in favor of a job completion mutation), then materialize exclusively through `insert_producer_verdict_in_txn`. |
| #39 | The spec says `adjudicate = {session, hold}`, but the actual pinned Gateway verb requires `{episode, action}` plus owner authorization and does not expose a direct set/clear-hold action. Direct SQL bypassed the owner and is now rejected. | Reconcile the governing spec with the real owner verb, or add an authorized owner action with the specified shape. |
| #43, #116 | `world.turn.window_start` has no consuming product API, and `Supervision` exposes `evaluate/5`, not the specified public `rail_step/4` input contract. | The P6 owner must expose/materialize the turn window and terminal watermark contract, including nil fallthrough and duplicate behavior, without a NULL watermark write. |
| #82, #83, #84, #85, #86, #129, #130, #131, #132, #134, #153 | The C5 scripts currently read test-controlled values; the runner copies a shell double instead of the real CLI, has no holder-checkout world, and cannot observe real git state, crash/EPERM, timeout cleanup, CWD, or laziness counters. | P4 must provide the real built `tightbeam rail-exec` path plus a spec-defined seeded-checkout materializer; the conformance lane must then drive real merge-base/diff/error/timeout processes and assert exact event/lifecycle records. |
| #133 | A sandbox profile-apply refusal cannot be caused by fixture script content. | P4 runtime must expose the deterministic fault-injection hook named by Q3; until then `contained-refused` remains case-level `pending-runtime`. |
| #89, #90, #91, #98, #99, #100, #101, #102, #103, #104, #105, #106 | The remaining C6 corpus compresses episode reclaim/reopen, action breadth, script-result, escalation union/fold, waiver revoke, fencing, retry, per-statute closure, rewake target, unbound-role, and digest behavior into non-executed shells. Several current TOMLs also fail the real rule schema or reference missing local scripts. | P5/spec owners must provide the missing §1.6 materializers and final rule shapes; then this lane must replace each shell with the full named matrix and drive it through `Rules.decide`, `Dispatch`, `RailRemedy`, and exact lifecycle assertions. |
| #108, #114, #115, #117, #118, #119, #120, #121 | The remaining C7 matrices need script execution, escalation/DR rows, retired-holder state, park-wake reuse, recovered-ruling cancellation, ruling consumption state, and the reconciled adjudicate verb. Current suppression branches also omit their declared lifecycle reasons. | P6/escalation/model-adjudication owners must expose those states and lifecycle records, including a spec-defined retired-session world shape and owner-authorized adjudication materializer; then the full sweep matrices can execute. |
| #123, #124, #125, #126, #127 | All five capstones need multi-edge, multi-phase composition across unresolved C5/C6/C7 seams; their current empty worlds cannot prove an end-to-end loop. | After the P4–P6 handoffs above land, P7 must supply the full-stack acting entry and this lane must author real reviewer, produced-test, real-run, YAGNI, and spec-review worlds with observable release/end states. |
| #141 | Pending fixture registration is now executable and case-level blockers are reported, but kind-specific semantic validation still depends on the missing §1.6 state shapes in #25. | Land the #25 vocabulary/schema amendment, then require every pending case to satisfy it before reporting. |
| #162 | The escalation trio is expressly blocked by `escalation-substrate-v1` adversarial review-clean. | Land and record that review-clean dependency, then execute the complete four-shape union at dispatch and sweep actors. |
