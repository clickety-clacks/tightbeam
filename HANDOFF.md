# Core assignments redo handoffs

- **Work-item 1 — assign-only `workItemId`:** `lib/tightbeam/assignments.ex` now ignores `work_item_id` for `dispatch`, but the required proof file is outside this lane. Update `test/assignments_test.exs` so the linked-dispatch case asserts the persisted and returned `workItemId` are `nil`, while the corresponding `assign` case still persists the link. Remove/adjust any `dispatch --work-item` CLI surface in `cli/src/*.rs` and prove the public dispatch path cannot add the field. The currently committed test asserts the forbidden behavior and therefore fails against the corrected implementation.
- **Work-item 48 — ordinary Dispatch audit only:** remove the remaining `metadata`/`composition` event taxonomy, derived work-item event projection, and public owner-partitioned work-item API from `lib/tightbeam/work_state.ex` and `lib/tightbeam/wire/router.ex`; update their tests to expect only the four v1 Dispatch verbs and ordinary `kind="verb"` audit. The owned `Assignments` and `WorkItems` callback emitters are already absent.
- **Check-tier 124 — complete zero-rules acceptance matrix:** add an explicit zero-rules test through `Dispatch.dispatch/3` in `test/check_tier_test.exs`. It must prove completion with zero verdicts succeeds, exercise the full v1 lifecycle authorization/state/error precedence matrix through Rules/Dispatch, and compare the complete lifecycle response against the v1 shape to show the check-tier delta is exactly always-present null `verdictKind` and `byUser` (accounting separately for later-spec P3 fields). No owned implementation change can supply this missing test proof.
- **P3 45 — full wrong-link Rules matrix:** extend `test/rules_test.exs` with review assignments linked to both the correct producer assignment and a different producer assignment, crossed with holder/third-session/user authors, producer/non-producer commissioners, and same/cross harness/provider stamps. Assert the exact `independent_`, `cross_harness_`, and `cross_provider_` fact results. The owned SQL helper excludes wrong links, but query-helper proof is not the required Rules fact matrix.
- **P3 79 — descendant termination proof:** `lib/tightbeam/producers.ex` now starts the exact `sh -lc` command only after its Port OS process is stopped, verified as its own process-group leader, and synchronously registered; cancellation/timeout signal that verified group, and cancellation-before-registration is retained until registration. Extend `test/producers_test.exs` with both cancellation and timeout cases whose commands create child and grandchild PIDs, then assert every descendant process is gone and no delayed end-state marker/verdict can appear. The existing 100 ms marker check does not close this clause.
- **P3 91 — public run-tests worker-to-gate path:** replace the private `Producers.__handle__/4` call in the produced-tests gate test with `Dispatch.dispatch/3` using `Gateway.handlers/1` configured with the test producer registry and named runner. Assert public `run-tests` returns `{:ok, %{queued: job_id}}`, the gate remains denied until that worker reaches `done` and files its producer verdict, and the same public completion call then closes the assignment. The existing test bypasses the required public verb chokepoint.
