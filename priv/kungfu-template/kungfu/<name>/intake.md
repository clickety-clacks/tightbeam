# <name> kungfu intake

Ask these questions in order. Record every answer at its named destination, or
record that the operator explicitly deferred it.

1. Which practiced capabilities should this kungfu provide, and what conversation
   signals should cause an offer?
   Destination: `identity/kungfu/<name>/capabilities.md`.
2. Which activities do its roles perform, what does each demand, and what ordered
   model ladder and floor should govern each one?
   Destination: `identity/kungfu/<name>/preferred-models.md`.
3. What kernel should govern the everyday role?
   Destination: `identity/guidance/<name>-role.md`.
4. What focused techniques should agents elect?
   Destination: `identity/skills/<name>-example/SKILL.md`, with ready skills elected
   in `identity/archetypes/<name>-role.toml`.
5. What enforceable law protects this way of working?
   Destination: `identity/rails/<name>-example.toml`.
6. Should `<name>-role` become the everyday default after adoption?
   Destination: apply the answer with
   `tightbeam config set default-archetype <name>-role`.
