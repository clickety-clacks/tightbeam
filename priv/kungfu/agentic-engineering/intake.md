# Agentic-engineering intake

Ask these questions in order. Record each answer at the named destination. The kungfu is not
operational until every answer is recorded or the operator explicitly defers that item.

1. What working-set capsule, activity-table row, and quality floor describes each available
   model for substrate-wide use?
   Destination: `guidance/preferred-models.md`.
2. What working-set capsule, activity-table row, and quality floor describes each available
   model for agentic-engineering work?
   Destination: `kungfu/agentic-engineering/preferred-models.md`.
3. What verdict kind and artifact kind name the org's completion papertrail, and what does
   verification mean in each source repository?
   Destinations: the org's chosen kinds in `identity/rules/verification.toml`; per-project
   verification prose in each repository's AGENTS.md (or equivalent).
4. What is the shared spec workspace root?
5. What review-independence stance governs model family, thinking level, and fresh-session
   separation?
