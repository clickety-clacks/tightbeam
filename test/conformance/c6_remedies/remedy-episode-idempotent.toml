[fixture]
class = "C6"
name = "remedy-episode-idempotent"
phase = "pending"
blocking_phase = "P5"
kind = "remedy"
source = "mechanism §C2 CAS; r9 occurrence rule"
legibility = "event:denied(rule=remedy-episode-idempotent);lifecycle:rail_remedy(outcome=claimed|dispatched|live|closed)"
[[fixture.rule]]
name = "remedy-episode-idempotent"
verb = "attest"
text = "remedy episodes publish once per occurrence"
edges = ["verb"]
effect = "remedy"
deny_when = [{ fact = "attest.kind", op = "eq", value = "completion" }]
[fixture.rule.remedy]
action = "assign"
produces = "reviewed-clean"
target_role = "reviewer"
[fixture.rule.remedy.params]
subject = "review {assignment_id}"
reviews = "{assignment_id}"
