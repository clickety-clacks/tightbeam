[fixture]
class = "C8"
name = "agent-question-carrier"
phase = "green"
blocking_phase = ""
kind = "question-carrier"
source = "coordination-fabric-v1 §7 input-needed carrier, §10 anti-adjudication; adjudication-deletion-amendment; GitHub #11"
legibility = "lifecycle:decision_request_asked"

# THE INVARIANT THIS FIXTURE EXISTS TO HOLD DOWN: an agent's question is DATA
# ITS ASKER CHOOSES TO HONOR, never a condition the substrate enforces
# (adjudication-deletion-amendment, "the tripwire for any implementer"). The
# deleted thing was never the rulable row — it was the substrate SEIZING work
# while one was open. So the case that matters most here is the negative one:
# an open question, filed against the very assignment its asker holds, and the
# completion of that assignment goes through untouched.
#
# The second failure this forbids is quieter and would look like tidiness: an
# answer taking the statute arm's vocabulary. `rule` reaching this row would
# turn a question into an allow/deny authorization the substrate then owns,
# with a ruling fact, a park wake, and a consume edge behind it. Refused by
# name, and the schema arm pins every one of those columns NULL besides.
#
# Every case crosses `Dispatch.dispatch/3` — `Rules.decide` sees `ask` and
# `answer` exactly as it sees every other verb. There is no [[fixture.rule]]:
# the carrier is substrate mechanism, and nothing here evaluates a statute.
