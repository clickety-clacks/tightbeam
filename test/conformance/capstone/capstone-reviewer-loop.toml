[fixture]
class = "Cap"
name = "capstone-reviewer-loop"
phase = "pending"
blocking_phase = "P7; reviewer teardown descoped: missing reviewer-retired/session-liveness observable and org rail"
kind = "capstone"
source = "smoke §3 capstone; Q2 named gap"
legibility = "event:denied(rule=capstone-reviewer-loop);lifecycle:rail_remedy; lifecycle:rail_sweep"
[[fixture.rule]]
name = "capstone-reviewer-loop"
verb = "attest"
text = "linked reviewed-clean from the review card's different-session holder releases completion"
edges = ["verb"]
effect = "remedy"
deny_when = [{ fact = "assignment.applicable_review_verdict_kinds", op = "not_in", value = ["reviewed-clean"] }]
[fixture.rule.remedy]
action = "assign"
produces = "reviewed-clean"
target_role = "reviewer"
[fixture.rule.remedy.params]
subject = "review {assignment_id}"
reviews = "{assignment_id}"
review_commit_refs = "{result_revision}"
