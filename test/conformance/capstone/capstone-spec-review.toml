[fixture]
class = "Cap"
name = "capstone-spec-review"
phase = "pending"
blocking_phase = "P7"
kind = "capstone"
source = "smoke §3 capstone spec-review judge"
legibility = "event:denied(rule=capstone-spec-review);lifecycle:rail_remedy"
[[fixture.rule]]
name = "capstone-spec-review"
verb = "attest"
text = "a spec-reviewed verdict on the work item releases the gate"
edges = ["verb"]
effect = "remedy"
deny_when = [{ fact = "work_item.verdict_kinds", op = "not_in", value = ["spec-reviewed"] }]
[fixture.rule.remedy]
action = "assign"
produces = "spec-reviewed"
target_role = "reviewer"
[fixture.rule.remedy.params]
subject = "review spec for {assignment_id}"
reviews = "{assignment_id}"
work_item = "{work_item_id}"
