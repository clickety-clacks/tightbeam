// CAPTURED SHAPE 2026-07-28 from GET api.openai.com/v1/models on a valid API
// key (the #89 api-key exercise, throwaway org): 200, 125 entries, bare ids
// under "data" — no display name, no reasoning levels, no context window.
// Trimmed to the two ids the exercise adjudicated live: codex-acp REFUSED
// `gpt-5.1-codex` at session/set_config_option (-32602 Invalid params) and
// ACCEPTED `gpt-5.6-sol`, which ran a real turn on the same adapter+auth.
// Right-sizing: assert only consumed fields and relied-on invariants; additive unconsumed fields must not fail.
{
  "data": [
    {
      "id": "gpt-5.1-codex",
      "object": "model"
    },
    {
      "id": "gpt-5.6-sol",
      "object": "model"
    }
  ]
}
